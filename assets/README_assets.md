# Assets to add for a stronger portfolio impression

Add these image files to this folder and then reference them in `README.md`:

1. `confusion_matrix.png`
   - export your final confusion matrix from the notebook

2. `training_curve.png`
   - export the final training/validation accuracy-loss chart

3. `sample_predictions.png`
   - show 4 to 9 test images with predicted labels

These images matter because recruiters scan visuals faster than notebooks.
Without them, your repo still looks like coursework.
